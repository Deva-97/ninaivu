enum UserRole { admin, agent, subAgent, customer }

enum SyncStatus { pendingCreate, pendingUpdate, pendingDelete, synced, failed }

enum PolicyStatus { active, expired, renewed, cancelled, pending }

enum FollowUpStatus { pending, completed, missed, cancelled }
