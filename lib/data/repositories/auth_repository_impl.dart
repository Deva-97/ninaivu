import 'package:insurance_reminders/domain/repositories/auth_repository.dart';

class AuthRepositoryImpl implements AuthRepository {
  // TODO(dev): Adapt AuthService + local/remote data sources into repository APIs.
}
